"use client";
import { useEffect, useState } from "react";
export default function OfflineBanner() {
  // Disabled offline banner - not needed for medical app
  // Users can still use the app offline, and messages will sync when online
  return null;
}
